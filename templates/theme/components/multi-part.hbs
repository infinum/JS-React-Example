import { createMultiStyleConfigHelpers, defineStyle } from '@chakra-ui/styled-system';

const parts = ['container', 'button', 'panel', 'icon'];

const { definePartsStyle, defineMultiStyleConfig } = createMultiStyleConfigHelpers(parts);

const baseStyleContainer = defineStyle({});

const baseStyleButton = defineStyle({});

const baseStylePanel = defineStyle({});

const baseStyleIcon = defineStyle({});

const baseStyle = definePartsStyle({
	container: baseStyleContainer,
	button: baseStyleButton,
	panel: baseStylePanel,
	icon: baseStyleIcon,
});

export const {{camelCase name}}Theme = defineMultiStyleConfig({
	baseStyle,
	// variants: {
	// 	ghost: {
	// 		container: {},
	// 		button: {},
	// 		panel: {},
	// 		icon: {},
	// 	}
	// },
	// sizes: {
	// 	sm: {
	// 		container: {},
	// 		button: {},
	// 		panel: {},
	// 		icon: {},
	// 	},
	// 	md: {
	// 		container: {},
	// 		button: {},
	// 		panel: {},
	// 		icon: {},
	// 	},
	// }
	// defaultProps: {
	// 	variant: 'solid',
	// 	size: 'md',
	// 	colorScheme: 'gray',
	// },
});
